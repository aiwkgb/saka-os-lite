const data=window.SAKA_DATA||{};function card(x){return '<div class=card><div class=tag>'+(x.priority||x.status||x.type||'')+'</div><h3>'+(x.name||x.title)+'</h3><p>'+(x.next_action||x.cta||'')+'</p></div>'}document.getElementById('projects').innerHTML=(data.projects||[]).map(card).join('');document.getElementById('opportunities').innerHTML=(data.opportunities||[]).map(card).join('');